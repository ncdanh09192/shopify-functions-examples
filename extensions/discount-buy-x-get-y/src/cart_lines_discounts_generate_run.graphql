query CartInput {
  cart {
    lines {
      id
      quantity
      cost {
        amountPerQuantity {
          amount
        }
      }
      merchandise {
        __typename
        ... on ProductVariant {
          id
          product {
            id
            # Returns true if the product has the "bxgy-eligible" tag
            hasAnyTag(tags: ["bxgy-eligible"])
          }
        }
      }
    }
  }
  discount {
    discountClasses
    # Config shape: { buyQty: number, getQty: number }
    # Set via Shopify Admin metafield or App Bridge
    metafield(namespace: "buy-x-get-y", key: "config") {
      value
    }
  }
}
